name: Patch desktop video poll parity

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-desktop-video-poll-parity.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply patch
        working-directory: _read_123_zip_20260821_180410
        run: python scripts/patch-desktop-video-poll-parity.py
      - name: Verify syntax and tests
        working-directory: _read_123_zip_20260821_180410
        run: |
          npm run check
          npm test
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add _read_123_zip_20260821_180410/server.js _read_123_zip_20260821_180410/tests/desktop-video-poll-parity.test.mjs
          git commit -m "Align desktop async video polling with browser runtime"
          git push
