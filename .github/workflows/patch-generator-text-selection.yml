name: Patch generator text selection

on:
  push:
    paths:
      - 'scripts/patch-generator-text-selection.py'
      - '.github/workflows/patch-generator-text-selection.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply repair
        run: python scripts/patch-generator-text-selection.py
      - name: Check
        working-directory: _read_123_zip_20260821_180410
        run: npm run check
      - name: Test
        working-directory: _read_123_zip_20260821_180410
        run: npm test
      - name: Commit repair
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add _read_123_zip_20260821_180410/styles/composer.css _read_123_zip_20260821_180410/index.html _read_123_zip_20260821_180410/tests/generator-text-selection.test.mjs
          git commit -m "Prevent generator chrome text selection"
          git push
